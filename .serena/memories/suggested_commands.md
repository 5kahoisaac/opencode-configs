make sync — sync config and commands to ~/.config/opencode and sync skills by default
make sync SKIP_SKILLS=1 — sync config only, skip skills
make sync-skills — reconcile installed skills with skills.csv
make help — list available make targets